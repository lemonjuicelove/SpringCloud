server:
  port: 9002

# nacos的配置
spring:
  application:
    name: nacos-payment-provider
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848 # 配置Nacos地址

        # server-addr: 192.168.39.128:81 # 配置Nacos地址

management:
  endpoints:
    web:
      exposure:
        include: '*'